class Person {
    name:String
    age:int
    
    fn __init(name, age) {
        $name = name
        $age = age
    }
    fn toString() {
        return $name+' '+str($age)
    }
}

fn main() {
    log(Person)

    p:=new Person('ccc', 52)
    log(str(p))
    log(p)
}
