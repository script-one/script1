class Person {
    fn __init(name, age) {
        $name = name
        $age = age
    }
    fn __str() {
        return $name+' '+str($age)
    }
}

log(Person)

p:=new Person('ccc', 52)
log(str(p))
log(p)
